# Prompts log

This file logs the tool/prompts you used and the results.  A small note on whether it was successful, choices you made and what was used.

## Example (can be deleted):

|AI tool name|prompt | result | alterations |
|---|---|---|---|
|gh copilot|what do you use to read in csv files in python| csv module or pandas | rejected pandas as it is not allowed |
|gh copilot|write a function that is passed the name of a csv file and will return the data set of that file| read_csv_file(file_name) | updated to account for utf-8 formatting |

## Team-Member1-Alejandra Pereira 


|AI tool name|prompt | result | alterations |
|---|---|---|---|
| gh Copilot| How can I calculate the next unique ID in a dataset?| you can do it taking the maximum ID from the dataset and incrementing by 1.| | 
|gh Copilot| I need to modify the new_olympic_athlete_events_result.csv file to include a new column called age, which must be added to the header  and include an empty value for each record|Since you're already calling process_medals_and_update_file() in the main(), it's better to add the age column at the end of this function after processing| Added logic inside process_medals_and_update_file() to append the age field to the header and ensure all rows contain the extra field.|
| gt copilot | How can I calculate the athlete’s age if I don't have the age information and the start date of the games in the same input file? | Since the input files do not directly provide the athlete’s age, it is calculated during processing by combining information from multiple files. The new_olympic_athlete_bio.csv file contains each athlete's birthdate, while the new_olympics_games.csv file includes the start dates of each Olympic edition. During the creation of new_olympic_athlete_event_results.csv, the program retrieves the athlete's birthdate and the corresponding edition start date, and computes the age by comparing these two dates. | adding helper functions to map athlete IDs to birthdates and edition IDs to start dates in order to implement the logic for age calculation and add it in the new event results file|
|chatgpt|How do I avoid duplicates when adding new rows to a dataset?|Suggested comparing unique keys (e.g., combinations of name, birth_date, and country_code for athletes).|check unique key combinations like athlete_id and born columns|
|chatgpt|How to handle missing values when processing CSV data?|Suggested leaving missing values as "" to maintain structural consistency. Applied this to fields like height, weight, and birth_date.|Handled missing values by using empty strings to keep data structure consistent|
|chatgpt|What is a memory-efficient way to iterate over a CSV data list and process all rows except the header, without using list slicing to skip it?|loop through the entire list and skip the header row by detecting it (for example, by checking the first row or using a flag). This avoids creating a new list (which slicing does) and thus reduces memory usage, especially with large datasets.| |
|chatgpt|How do I skip the header using a flag?|You can skip the header using a flag by setting a boolean variable before the loop. Inside the loop, you check if the flag is True—if it is, you know the current row is the header, so you skip it and then set the flag to False| Used a boolean flag to skip header row during iteration|
|chatgpt|How can I get the indices of the "athlete_id" and "born" columns from a CSV header row, and then use those indices to extract values from all the data rows?|Use the .index() method on the header list to find the positions of "athlete_id" and "born". Then iterate over each data row, and use those indices to get the corresponding values.| Used header.index() to find column positions, then extracted values from data rows using those indices|
|chatgpt|I have a list of Olympic editions where each edition has a field called competition_date containing a date range string in the format "dd-Mon-yyyy to dd-Mon-yyyy". How can I extract the start date and end date from the competition_date string and use them to calculate ages|You can extract the start and end dates by using .split() method (.split(" to ")) to separate the two dates in the string. Then, use datetime.strptime to parse each date string into a datetime object. Once you have these two datetime objects (for start and end dates), the best way to store them together efficiently is by putting them in a tuple.| |
|chatgpt|How can I check if someone’s birthday has already happened in the year of an event, so I can calculate their correct age?|you create a new date for their birthday but in the event’s year. You can use Python’s datetime.replace() method to keep the same month and day but change the year. For example, if you have their birth date as a datetime object, you do birthday_this_year = birth_date.replace(year=start_date.year). Then you compare the event’s start date with birthday_this_year: if the event starts before their birthday, you subtract one from their age. This way, you get the correct age accounting for whether their birthday has passed yet that year.| Used datetime.replace() to adjust birth year and compare with event date for accurate age calculation |
|chatgpt|I’m working with Olympic athlete event results from various sources such as Paris medallists, team details, athlete birthdates, and events, and need to merge all this data into one dataset. What is the best way to organize the process so I can prepare all necessary data, calculate fields like age, avoid duplicates, and build each output row?|The best way to organize this process is: Extract and prepare all relevant data upfront: Collect athlete info, team memberships, birthdates, and event dates into easily accessible structures like dictionaries. Use helper functions to build each output row. Then ese helper functions to build each output row including calculated fields like age. Manage duplicates efficiently: Maintain a set of processed athlete-event pairs so you can skip duplicates and ensure data integrity as you append rows.|Used dictionaries and helper functions to build rows with calculated fields.|
|chatgpt| How can I process Olympic medal data to count unique medals and athletes and grouping them by country and edition?|Parse the data from your CSV or dataset, ensuring you separate the header from the actual data rows. Create a dictionary structure to store the medal tally, organized first by edition (e.g., "2024 Paris") and then by country NOC code.For each athlete event record, extract: The edition name and ID, The country NOC, The athlete ID, The medal type (Gold, Silver, Bronze). For each edition and country, maintain a running tally: Use a set to keep track of unique athlete IDs (to count distinct athletes) and counters for each medal type to accumulate counts, After processing all records, transform the dictionary into a summary list or table. Write the summary data to a CSV file|Implemented nested dictionary structure for easier aggregation in the process_medal_tally function.|
|chatgpt|How can I handle February 29 birthdays properly when calculating age?| A practical and efficient way is to try replacing the birth year with the event year directly; if it raises a ValueError (non-leap year), catch it with try-except and adjust the day to February 28 to calculate age correctly.| Used try-except to handle Feb 29 birthdays in non-leap years, adjusting day to Feb 28 when needed.|


## Team-Member3-Manpreet Singh


|AI tool name|prompt | result | alterations |
|---|---|---|---|
| ChatGPT | How can I safely merge new medal tally data into existing without duplicates? | Checking for existing IDs before appending new rows, using sets for quick lookup. | Implemented a merge function that appends only new entries based on unique 'id' fields to avoid duplication. |
| ChatGPT | What is a Pythonic way to write `col_index()` that handles missing columns gracefully? | Recommended try-except around `header.index(col_name)` returning -1 if not found. | Added logging for missing columns in prototype. |
| copilot | How to improve `is_duplicate_athlete()` for partial matches? | Possible fuzzy matching on names or approximate date checks using string similarity or date tolerances. | |
| ChatGPT | How to reliably get max numeric ID from CSV data when some IDs might be missing? | Try-except during int conversion, skipping invalid entries, returning max found or zero default. |  |
| ChatGPT | How to efficiently update medal counts by country when adding new results? | By using a dictionary keyed by country codes (NOCs) to accumulate medal counts, updating counts as new data streams in. | Implemented a dictionary-based accumulator for medal tallies to allow quick incremental updates without reprocessing entire datasets. |
| chatGPT | How to get the index of a column name in a CSV header row in Python? | Used header.index(column_name) to return the position of a given column. |	Added a try-except block to catch ValueError if the column is missing. |
| chatGPT | How to reliably get max numeric ID from CSV data when some IDs might be missing? | Loop through ID column, use int() with try-except, track max. | Filtered out non-digit IDs and handled empty files by returning 0. |
| Copilot | How to check if a dictionary representing a row already exists in a list based on specific fields? | Compared field values (like name, dob, noc) to detect duplicates. | Customized to check only name, dob, and noc fields for is_duplicate_athlete. |
| Copilot | How to merge two CSV datasets but avoid duplicates based on certain fields? | Iterate through both lists, check for duplicates before appending new data. | Integrated logic to compare key athlete fields before appending Paris data. |
| chatGPT | How to calculate the next edition ID for the Olympics dataset? | Sort existing edition_ids and return max + 1. | Ensured only numeric IDs are considered, added default if none found. |
